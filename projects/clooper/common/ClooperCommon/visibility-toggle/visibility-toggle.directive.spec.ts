import { VisibilityToggleDirective } from './visibility-toggle.directive';

describe('VisibilityToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibilityToggleDirective();
    expect(directive).toBeTruthy();
  });
});
